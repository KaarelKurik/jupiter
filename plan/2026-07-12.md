# 2026-07-12 — plan/ reorg; allocation kill (inference recursion)

- **plan/ directory**: plan-claude.md split into dated session logs +
  `plan/claude.md` resume point; kaarel's journal moved verbatim to
  `plan/kaarel.md`, still gitignored (rule narrowed `plan` → `/plan/kaarel.md`
  so the rest of the directory is tracked).
- **Per-ray allocation 0.7–1.4 MB → 1.5 kB (3 allocations)**; knot-hitting ray
  5.5 → 2.8 ms, trefoil 384×288 render 14.8 → 10.1 s. One root cause: method
  self-recursion inference widening on the nested jacobian chain
  (`jacobian_columns → collar → surface_normal_out → jacobian_columns`) boxed
  the whole step loop. Full story + the second appearance of the same trap
  (BVH recursion with a Union accumulator) in measurements.md, 2026-07-12
  entry. Riding along: `AmbientRay{P,V}`, concrete `RayBudget`/`Camera`,
  parametric `Scene`, allocation-free recursive BVH traversal, explicit tuple
  indexing in `next`/`twin`/`prev`. The 3 remaining allocations are the
  per-passage sum-type boxes at the honest boundaries — by design, ~the
  algorithmic floor. Certified: 219/219 cold, physics_diff 0 flips, ≤1.8e-10.
