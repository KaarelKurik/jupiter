# 2026-07-04 — design review

- Review found mechanical bugs in `half_edge_ccw`, `chart_transition`,
  `christoffel` tensor assembly (index permutation re-verified), `inner_metric`
  fundamental form + eltype promotion, normal normalization — all fixed.
- Reference-frame design gap raised: how chart transitions land in the
  neighbor's frame was underspecified. Resolved 2026-07-08 by
  `half_edge_offsets` + two-sided rotation in `chart_transition` (verified).
