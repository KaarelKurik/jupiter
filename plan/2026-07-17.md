# 2026-07-17 — notes as a snapshot, an atlas for kaarel's grip, and a requirements audit

- **Session goal** (kaarel): improve fine legibility / coarse organization of
  code and notes, with emphasis on giving kaarel enough grip on the code that
  his sanity injections stay useful. Notes first, by agreement.
- **claude.md rewritten as a present-state snapshot.** The "Where we are"
  section had become a five-session chronicle duplicating the session logs
  and measurements.md, and the 2026-07-11 "Performance roadmap" actively
  contradicted the state above it (still named tabulated Γ the top lever and
  the GPU port future work). Now: present-tense state, pruned next
  candidates, roadmap dissolved into them, history left to the dated logs
  and jj. Standing rule added to conventions: claude.md stays a snapshot.
- **atlas.md added** (repo root, rendered as a private artifact): the
  cross-file layer per-file comments can't carry — math-to-code dictionary
  (wedge complex, blending, collar/metric, halves), a computed chart-wedge
  figure at n=4 and n=7 (true images under square_coords_to_chart, inline
  theme-aware SVG), a ray's life with the two drivers, the
  three-implementations-one-truth derivative story and certification tiers,
  precision seams, instrument table, file map, pre-edit hazards. Update
  trigger: structural change only. The figure caught a convention slip
  within the hour (kaarel had the canonical half-edge as unit length; it's
  2^(−2/n), the √2 normalization pins the face's far *corner* to the unit
  circle) — that fact now lives in the caption.
- **Kaarel's corrections to the atlas**: (1) ambient spaces of the two
  halves are conceptually shareable (both mouths in one room); checked the
  code — placements permit it, but Scene keys one mouth + the sky by side
  and trace_ray re-enters only mouths[side], so a co-placed second mouth is
  invisible to exiting rays; recorded under the multi-mouth item. (2) The
  fitting grid is honestly described as each wedge inducing a 4×4
  quarter-step grid (= twice-CC-refined vertices) sharing seam columns and
  the origin ⇒ 12n+1 points; 3×4-per-wedge is just the stored dedup.
- **Requirements audit** (kaarel asked which of his recorded requirements
  might be XY problems). Outcomes, all recorded in claude.md:
  - **Level-set C³ demand → probe question** (kaarel skeptical it changes
    the answer, but probing is cheap). The case: step-4 data shows
    conditioning at step scale, not smoothness class, predicted tabulation
    quality (exp-flat C^∞ plateaus at 1e-3, C⁴ nonic reaches 1e-5), and the
    acceptance instruments judge exit maps, not analytic class — so sub-C³
    candidates (superfrusta's a.e.-differentiability) get probed, not gated.
  - **Bit-identity = landing instrument, not permanent semantics** (agreed):
    "retire served pins" is a next candidate — merge the
    sweep_ray/trace_geodesic op-for-op twins into one controller body
    (identity by construction), decide whether the recursive renderer is a
    production path or a certification twin.
  - **Kaarel's secondary point, promoted to an open item: no
    convergence-established ground truth exists.** Every instrument compares
    same-discretization twins (reference vs production at the same budgets;
    precision_diff's "truth" = F64 production at production budget; F32
    accuracy relative to F64 at the same h/tol). Correct for isolating
    precision, but discretization error itself has never been quantified —
    "truth"/"accuracy" in the record means "our best at the same h."
    Richardson-style probe (tighten h/tol until exit-map quantiles
    stabilize) when wanted.
  - **F64 mouth entry: provenance corrected** — it was a 14c design
    convenience, not a kaarel requirement, and was never measured as
    necessary; droppable if the host entry stage ever blocks full-device
    rendering.
  - **Small flags, agreed**: depth-blend "forever" softened (written when
    tabulation made it free); f(x)+f(1−x)=1 marked non-load-bearing
    (surface() renormalizes regardless) in the fork notes and the
    blend_scalar docstring — it must not gate fork blend candidates.
  - **Suspected and cleared**: raw emit_ray effort semantics (consumed by
    coast's parameter continuity, and kaarel wants the choice explicit —
    2026-07-13), reference-first discipline, the no-sup decomposition
    metric, probe disposability, connection transport.
- **Code candidates carried forward, not done**: integrator-twin merge +
  recursive-renderer status (now a next candidate), geodesic.jl/render.jl
  file splits, fit.jl header, env comment at situate (env rationale
  recorded in claude.md architecture section meanwhile).
- Only code touched: one docstring (chart.jl blend contract). No runtime
  change; no certification run needed.
