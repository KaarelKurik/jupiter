# Session 2026-07-18 — execution-engineering pivot: phase 1 lands (ABI-stack diagnosis, @inline collapse), flyvideo grows gpu=1, a latent F32 crash falls out

Why-chains and numbers live in measurements.md 2026-07-18 and 2026-07-18b;
this is the session record. Two jj changes landed (phase 1; flyvideo gpu=1 +
NaN guards) plus this wrap.

- **DNGR bundles, kaarel's reading confirmed** (he checked the paper): one
  bundle per pixel, deviation ellipse filters the star field — quality, not
  sparsity. The duality (quality-per-ray ↔ rays-per-quality) survives, but
  our sparse-ray use runs the linearization at inter-ray spacing, where
  validity must be *checked*, not assumed. Design consequences folded into
  plan item 1: pairwise Hermite consistency + discrete (side, passage)
  agreement as the trust gate — never pointwise κ alone; land the
  architecture on finite-difference bundles before extending jets to
  order-4; speedup claim stated conditionally (10–100x smooth frames,
  ~break-even-but-antialiased at peak crossing).
- **Kaarel's loop-cut cache idea, assessed and adopted into item 2**: it is
  a Poincaré section family (Birkhoff's construction) — (loop cuts) ×
  (d-interval) with every trapped geodesic crossing infinitely often. Key
  wins over the planned time-parametrized 4D table: the ḋ≈0 trapped set
  becomes *interior* to the table domain (the singularity was a time-
  parametrization artifact), and the cylinder product structure collapses
  the table to a 2D-domain return map (s,θ)→(s′,θ′,loop,σ) with d, ḋ exact.
  Where per-crossing error beats per-winding RK4 error, the cache is *more*
  accurate than integration (Lyapunov amplification hits both identically).
  Softens the cost analysis's "irreducible core" to Lyapunov-limited depth.
  Caveats recorded: per-cell Jacobian as trust flag, tangency boundaries,
  d-free region only, sojourn-bound probe for choosing the loops.
- **The pivot (kaarel's call)**: bundles and caches set aside; GPU execution
  engineering active now — see how far pure execution goes. Kaarel's
  scoring correction: offline renders benefit too via the GPU path (device
  F32 already ahead of CPU at production resolution), so the finish line is
  not realtime-authoring-only.
- **Phase 1 (measurements.md 2026-07-18)**: profile-first, no ncu/nsys on
  the box — host-side stage instrumentation + PTX census. Findings: wall ≈
  90% kernel (per-round entry solves 0.5%; the old "~20% host" was the
  initial pool fill); kernel = throughput-bound round 1 + latency-bound
  straggler tail (46% of kernel for <1% of rays — the budget-capped
  filament pixels, ~50 ms per 64-attempt round *independent of ray count*);
  occupancy ruled out twice (register caps always lose — bandwidth-bound on
  local traffic); the 19.2K local/thread was **ABI call-frame stack**
  (depots 656 B), the 07-16 "integrator state" attribution corrected.
  Lever: @inline collapse of the christoffel tree into flow6 — kernel
  0.560→0.386 s (−31%), CPU renders ~15%, local 19.2K→14.0K. Blowup
  boundary mapped: full or structural-layer inlining unions branch frames
  to 127–188 KB/thread and OOMs. Certified: 451 cold green; physics_diff 0
  flips, deviations moved in-band (muladd-fusion shift — the 07-16
  "stacking reordering" case; next deliberate reordering is the re-baseline
  moment); device parity holds; device pre/post 0 flips, q99 4e-7.
  threads=128 a free ~6% (finer blocks spread tail rounds across SMs).
- **Kaarel's catch: the space flythrough never used the card** — nor the
  wavefront driver, nor F32 (keyframe_raymap → recursive/F64/CPU). flyvideo
  now takes gpu=1 (measurements.md 2026-07-18b): DeviceSweep frames, F32
  tracing, F64 entry solves; coast/probes/escalation deliberately CPU F64
  (flight path bit-identical, escalated pixels land F64). First F32 flight
  found a **latent crash in the certified CPU F32 wavefront** (NaN ray →
  wedge_index InexactError; in-kernel it kills the launch; 14c acceptance
  had only static cameras — an acceptance-sweep gap, same overlooked-layer
  flavor). Fixed with control-flow-only guards: non-finite rays retire as
  RAY_UNRESOLVED into the standing escalation machinery. physics_diff
  deviations *exactly* unchanged; flight frames GPU-vs-CPU ≤1 LSB on ~5%
  of pixels.
- **Numbers**: trefoil 768×576 approach, equal 420 s windows: GPU 86 vs
  CPU 37 frames (~2.3x, GPU window carrying heavier startup); cube 192×144
  flight: CPU wins outright. gpu=1 is a production-resolution tool.
  Workload shift confirmed: mid-crossing frames are all-throat (static
  camera enters 15%) — **future execution benchmarks run against flight
  segments, not the static camera**.
- **Doctrine discussion (kaarel)**: tidy-semantics minimalism is not the
  goal — reference.jl exists so production can fuse into opaque fast
  blocks. Agreed, with the session's correction that the first 31% required
  no opacity (ABI artifact). Agreed opening move for next session:
  **Γ-contraction fusion** — never materialize the 27-component Γ; compute
  w_u = v^i v^j (∂_i g_{uj} − ½ ∂_u g_{ij}), a = −g⁻¹w, specialized to the
  hot-path w ≡ v (general transport stays for camera frames). Real FLOP
  reduction, not just traffic. reference.jl first, production follows,
  re-baseline physics_diff. Behind it: sweep_ray's 7.9K frame, pool-init
  overlap. Persistent threads demoted (tail is latency-bound, not
  wave-quantized).
- **Tools ask recorded**: `pacman -S nsight-compute nsight-systems` (both
  in extra) — ncu for hardware counters (stall reasons would confirm the
  local-latency story directly), nsys for the host/device timeline (the
  right instrument for pool-init overlap work).
- **Deferred/unprobed**: adaptive-mode flights with escalation (still only
  uniform exercised); jd daemon --restart/--stop + warm≡cold check; the
  honest constant-speed cube video; root cause of *which* F32 op overflows
  in the NaN blowup (guard makes it moot for correctness; probe if the
  unresolved-rate at F32 ever looks anomalous).
